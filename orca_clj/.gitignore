/out/
/.cpcache/
/.clj-kondo/.cache/

# Compiled Stan artifacts: keep the .stan sources, ignore everything CmdStan
# generates from them (executables with no extension, plus .hpp/.o).
/stan/*
!/stan/*.stan
*.hpp
*.o
